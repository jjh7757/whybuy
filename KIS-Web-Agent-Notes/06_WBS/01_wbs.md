# WBS — 개발 작업 분해

- 작성일: 2026-08-06
- 총 Must: 약 21h

---

## 0. 분해 원칙

| 원칙 | 적용 |
|---|---|
| 한 작업은 4h를 넘지 않는다 | 최대 작업이 2.5h |
| "만든다"가 아니라 "무엇이 되면 끝인가"로 쓴다 | 각 작업에 완료 조건 |
| 검증이 필요한 작업은 별도 작업으로 분리 | 13번(주문 검증), 20번(배포 검수) |
| 되돌릴 수 없는 것을 먼저 검증한다 | 2번(KIS 연결)이 첫 작업 |

---

## 1. 기반 (WBS 1.x) — 4.4h

| ID | 작업 | 예상 | 완료 조건 | 선행 |
|---|---|---:|---|---|
| 1.1 | Next.js 프로젝트 생성 + Supabase 클라이언트 연결 | 0.5h | 로컬에서 `supabase.from('...')`가 응답한다 | — |
| 1.2 | GitHub 리포 + Vercel 연동 + **빈 화면 배포** | 0.5h | 🔴 **공개 URL이 존재하고 열린다** | 1.1 |
| 1.3 | KIS 자격증명을 n8n에서 꺼내 `.env.local`·Vercel에 등록 | 0.3h | 양쪽에 `KIS_*` 5개가 있다 | 1.2 |
| 1.4 | 🔴 `lib/kis.ts` — 토큰 발급 + `kis_tokens` 캐시 + 1회 재시도 | 2h | **배포본에서 토큰이 발급되고 재사용된다** | 1.3, 2.1 |
| 1.5 | Supabase 테이블 6개 + RLS 정책 | 0.8h | 6개 테이블이 있고 `kis_tokens`·`user_wallets`가 anon으로 안 읽힌다 | 1.1 |
| 1.6 | `stocks` 코스피 50종목 CSV Import | 0.3h | 50행 이상, `stock_name` 검색이 된다 | 1.5 |

> 🔴 **1.4가 이 프로젝트 전체의 임계 경로입니다.** 화면 코드를 한 줄도 쓰기 전에 끝냅니다.

---

## 2. 계좌 화면 (WBS 2.x) — 5.0h

| ID | 작업 | 예상 | 완료 조건 | 선행 |
|---|---|---:|---|---|
| 2.1 | `/api/account` — KIS 잔고 조회 | 0.7h | JSON으로 예수금·보유종목이 온다 | 1.4 |
| 2.2 | `/` 대시보드 화면 — 표 렌더링 | 0.8h | 숫자가 표로 보인다. 보유 0개여도 안 깨진다 | 2.1 |
| 2.3 | `lib/gemini.ts` + `/api/ai/explain` (AI-1) | 1h | 같은 입력이라도 매번 새 문장이 온다 | 1.3 |
| 2.4 | 해석 버튼 + 로딩·실패 UI | 0.5h | 실패해도 숫자는 남는다 | 2.2, 2.3 |
| 2.5 | 🔴 Google Cloud OAuth 동의 화면 + Supabase Auth Provider 등록 | 0.7h | Supabase Auth 대시보드에서 Google 로그인 테스트가 성공한다 | 1.2 |
| 2.6 | 🔴 로그인 버튼 + 콜백 라우트 + `lib/wallet.ts` (`user_wallets` 조회/생성 + 예산 계산) | 1.3h | 로그인 후 새로고침해도 세션이 유지되고, 같은 계정이면 같은 `user_id`를 돌려받는다 | 2.5, 1.5 |

---

## 3. 종목·주문 화면 (WBS 3.x) — 5.9h

| ID | 작업 | 예상 | 완료 조건 | 선행 |
|---|---|---:|---|---|
| 3.1 | `/api/stocks` — 부분 일치 검색 | 0.5h | `삼성`으로 후보 여러 건이 온다 | 1.6 |
| 3.2 | 검색 UI + 모호성(2.2)·미발견(2.3) 처리 | 0.5h | 자동 선택하지 않는다. 0건이면 예시가 나온다 | 3.1 |
| 3.3 | `/api/quote` — KIS 시세 조회 | 0.5h | 현재가·등락률·거래량이 온다 | 1.4 |
| 3.4 | 시세 표시 + AI 해석 버튼 | 0.5h | 표 + 해석 문장 | 3.3, 2.3 |
| 3.5 | 주문 폼 — 수량·예상금액·근거 6개 라디오 | 1h | 🔴 **근거 미선택 시 버튼 비활성화** | 3.4 |
| 3.6 | 확인 모달 (미로그인 시 Google 로그인 버튼 / 로그인 시 남은 예산 표시) | 0.9h | 모달에 종목·수량·금액·근거·남는 예산이 요약된다 | 3.5, 2.6 |
| 3.7 | 🔴 `/api/order` — 14단계 검증 + 주문 실행 | 2h | 검증 순서대로 각각 다른 상태코드가 온다 | 3.3, 2.1, 2.6 |

### 3.7의 세부 (별도 관리)

| 단계 | 내용 | 실패 시 |
|---|---|---|
| ① | 입력 검증 | 400 |
| ② | 🔴 로그인 세션 확인 | 401 |
| ③ | 예산 조회/생성 | 500 |
| ④ | 장 시간 (`lib/market.ts`) | 422 |
| ⑤⑥⑦ | 종목·시세·실제 예수금 | 404 / 502 / 422 |
| ⑧⑨ | 🔴 가상 예산·중복 | 422 / 409 |
| ⑩⑪ | `orders` → `rationales` INSERT | 500 |
| ⑫⑬⑭ | KIS 주문 → UPDATE → 이벤트 | `rejected` 기록 |

---

## 4. 회고 화면 (WBS 4.x) — 4.2h

| ID | 작업 | 예상 | 완료 조건 | 선행 |
|---|---|---:|---|---|
| 4.1 | `/journal` — `orders ⨝ rationales` 목록 (`user_id` 필터) | 1.2h | 최신순 표가 나오고 **다른 방문자 주문은 안 보인다** | 3.7 |
| 4.2 | 당시가 vs 현재가 대조 | 0.8h | 🔴 **KIS 실패 시 `—`로 표시되고 목록은 남는다** | 4.1, 3.3 |
| 4.3 | 상단 집계 (총 건수·기록률·`gut` 비율) | 0.7h | 숫자 3개가 표시된다 | 4.1 |
| 4.4 | `/api/ai/review` (AI-2) + 버튼 | 1.5h | 근거 목록이 바뀌면 코멘트가 바뀐다 | 4.1, 2.3 |

---

## 5. 사용성·관측 (WBS 5.x) — 3.0h

| ID | 작업 | 예상 | 완료 조건 | 선행 |
|---|---|---:|---|---|
| 5.1 | 첫 화면 안내 + 버튼 3개 | 0.5h | 서비스 설명 1문장 + 진입 버튼 3개 | 2.2 |
| 5.2 | 빈 상태 화면 3종 (보유 0 / 검색 0 / 기록 0) | 0.5h | 각각 다음 행동을 안내한다 | 4.1 |
| 5.3 | 예외 UI 12종 정리 + 로딩 상태 | 1h | [[../02_Domain/03_workflow\|흐름]] 3절 표의 12행이 전부 화면에 있다 | 전체 |
| 5.4 | `lib/events.ts` + 이벤트 12개 삽입 | 0.7h | `event_logs`에 12종이 쌓인다 | 1.5 |
| 5.5 | 측정 SQL 4개 실행·저장 | 0.3h | 4개가 각각 결과를 반환한다 | 5.4 |

---

## 6. 검증·녹화 (WBS 6.x) — 2.3h

| ID | 작업 | 예상 | 완료 조건 | 배치 |
|---|---|---:|---|---|
| 6.1 | 🔴 **장중 실제 주문 1건 통과 확인** | 0.7h | `submitted` 1건 + `rationales` 1건 | 🔴 Day 2 |
| 6.2 | 🔴 **주문 시연 녹화** | 0.3h | 영상 존재 + 계좌번호 미노출 | 🔴 Day 2 15:30 前 |
| 6.3 | 장 마감 주문 차단 검증 | 0.2h | 휴장일에 시도 → 차단 | Day 3 |
| 6.4 | 예외 12종 통합 테스트 | 0.6h | 완료 기준 4절 전 항목 | Day 3 |
| 6.5 | 🔴 **시크릿 창·휴대폰 접속 검수** (Google 로그인 포함) | 0.5h | 화면 3개가 전부 보이고 로그인이 완료된다 | 🔴 Day 4 |

---

## 7. 문서·발표 (WBS 7.x) — 3.2h

| ID | 작업 | 예상 | 배치 |
|---|---|---:|---|
| 7.1 | README 작성 (실행 방법·기능·한계) | 1h | Day 4 |
| 7.2 | 🔴 **범위 축소 설명서** (REQ-01 필수) | 1h | Day 4 |
| 7.3 | 조회·회고 시연 녹화 | 0.5h | Day 4 |
| 7.4 | 발표자료 + 리허설 | 0.7h | Day 4 |

---

## 8. 임계 경로

```
1.1 → 1.2 → 1.3 → 1.4 ★ → 2.1 → 3.3 → 3.7 ★ → 6.1 ★ → 6.2 ★ → 4.1 → 4.4
              ↓     (2h)                (2h)     (0.7h)  (0.3h)
        2.5·2.6(Google 로그인·예산, 2h)
```

**★ 표시가 지연되면 전체가 밀립니다.**

| 작업 | 지연 시 | 대응 |
|---|---|---|
| 1.4 KIS 연결 | 🔴 주제 자체가 위험 | 8/7 12:00에 [[../01_Baseline/02_project_baseline\|대안 시나리오]] 발동 |
| 3.7 주문 API | 주문 검증 불가 | 사전 검증 일부를 Day 3로 미루고 정상 경로만 먼저 통과 |
| 6.1/6.2 검증·녹화 | 🔴 **복구 불가** | 8/7 15:30이 절대 데드라인 |

---

## 9. 병렬 가능 / 불가능

| 병렬 가능 | 이유 |
|---|---|
| 2.3(Gemini)와 1.4(KIS) | 서로 독립적입니다 |
| 5.1·5.2(사용성)와 4.x(회고) | 화면이 다릅니다 |
| 7.x(문서)와 개발 | 문서는 언제든 씁니다 |

| 병렬 불가 | 이유 |
|---|---|
| 1.4 이전의 모든 KIS 작업 | 토큰이 없으면 아무것도 안 됩니다 |
| 3.7 이전의 4.x | 주문 데이터가 없으면 회고 화면에 보여줄 것이 없습니다 |

> 🔴 **1인 프로젝트이므로 병렬은 "순서를 바꿔도 된다"는 의미입니다.** 막혔을 때 다른 작업으로 옮겨 시간을 버리지 않기 위한 정보입니다.

---

## 10. 합계

| 그룹 | 시간 |
|---|---:|
| 1. 기반 | 4.4h |
| 2. 계좌 화면 | 5.0h |
| 3. 종목·주문 | 5.9h |
| 4. 회고 | 4.2h |
| 5. 사용성·관측 | 3.0h |
| 6. 검증·녹화 | 2.3h |
| 7. 문서·발표 | 3.2h |
| **합계** | **28.0h** |
| 가용 | 28h |
| **차이** | **0h** |

### 🔴 가상 예산 + Google 로그인이 추가되며 다시 맞춘 계산입니다

처음에는 익명 쿠키로 방문자를 구분할 계획이었지만, **지속 가능한 서비스를 위해 Google 로그인(Supabase Auth)으로 바꿨습니다.** 로그인 없이는 예산도 회고 기록도 기기를 넘기면 끊기기 때문입니다.

| 변경 | 항목 | 시간 |
|---|---|---:|
| 🆕 Google OAuth 설정 (Cloud Console + Supabase Provider) | 2.5 | +0.7h |
| 🆕 로그인 버튼·콜백·가상 예산 로직 (`user_wallets`) | 2.6 | +1.3h |
| 🆕 회고 화면 사용자 필터 | 4.1 | +0.2h |
| 확인 모달 단순화 (패스코드 입력란 삭제) | 3.6 | -0.1h |
| 종목 100 → 50개 (기존 계획된 트림) | 1.6 | -0.2h |
| 예외 UI 정리 범위 축소 | 5.3 | -0.5h |
| 주문 폼 단순화 | 3.5 | -0.5h |
| 발표 준비 시간 축소 | 7.4 | -0.8h |
| 통합 테스트 범위 축소 | 6.4 | -0.2h |
| 테이블 6개로 증가 | 1.5 | +0.1h |
| **순변화** | | **0h** |

익명 쿠키 방식 기준으로 이미 28.0h에 맞춰 놨던 계획에서, **Google 로그인으로 바꾸며 늘어난 시간(+2.3h)을 예외 UI·주문 폼·발표 준비·통합 테스트에서 조금씩 줄여(−2.3h) 다시 정확히 28.0h로 맞췄습니다.**

발표 시연에 쓰는 종목은 3~5개입니다. 50종목이면 충분하고, 확대는 이미 `Could`입니다.

> 처음부터 여유 있게 잡지 않고 부족한 상태를 드러낸 뒤 조정합니다. **부족을 감추면 Day 4에 드러납니다.** 버퍼가 정확히 0이므로, [[02_milestones|마일스톤]]의 위험 목록 중 하나라도 현실화되면 즉시 Could 항목(AI-3 용어 설명 등)을 보류합니다.

---

## 11. 🔄 계획 대비 실제 (8/7 기준)

> **이 절만 사후에 추가했습니다.** 위 1~10절은 **계획 시점의 기록**이라 덮어쓰지 않았습니다. 계획이 얼마나 빗나갔는지가 회고 재료이기 때문입니다.

### 11.1 임계 경로가 하루 만에 뚫렸습니다

계획한 임계 경로는 `1.1 → 1.2 → 1.3 → 1.4 → 2.1 → 3.3 → 3.7 → 6.1 → 6.2 → 4.1 → 4.4`였고, **Day 1~2 이틀에 걸칠 예정**이었습니다.

**실제로는 8/6 하루 오후(13:20~16:13)에 전 구간을 통과했습니다.**

| 작업 | 계획 | 실제 |
|---|---|---|
| 1.x 기반·`lib/kis.ts` | Day 1 | 8/6 오후 ✅ |
| 3.1~3.7 종목·시세·주문 | **Day 2** | **8/6 16:13** — 하루 빠름 |
| 6.1 장중 실주문 통과 | **Day 2 (데드라인 8/7 15:30)** | **8/6 15:16** — 매수 주문번호 `0000041001` |
| 4.1~4.4 회고 화면 | **Day 3** | **8/6 15:40** — 이틀 빠름 |
| 5.x AI 2종 | **Day 3** | **8/6 15:34~15:59** — 이틀 빠름 |
| 7.2 범위 축소 설명서 | Day 4 | 8/6 |
| 7.3 제출 문서 | Day 4 | 8/7 17:37 |

> 🔴 **"불확실한 것을 먼저"가 통한 결과입니다.** 가장 위험했던 `1.4`(Vercel×KIS 연결)가 첫날 오후에 뚫리자 나머지가 연쇄적으로 풀렸습니다. 132행의 "1.4가 지연되면 8/7 12:00 대안 시나리오 발동"은 **발동할 일이 없었습니다.**

### 11.2 계획에 없던 작업이 WBS 밖에서 늘었습니다

Day 2(8/7) 하루는 거의 전부 **WBS에 등재되지 않은 작업**이었습니다.

| 추가된 것 | 원래 WBS에서 |
|---|---|
| 정량지표·인기 종목·종가 그래프(일/주/월/년+분봉+호버) | 없음 |
| 배당·재무비율·탭 구조 | 없음 |
| 지정가 주문 + 체결확인 + 취소 + 호가단위 검증 | 없음 (`3.7`이 14단계 → 매수/매도 × 시장가/지정가로 확장) |
| 매도 주문 + 매도 근거 5종 | Won't Have였음 |
| 첫 방문자 튜토리얼 | 없음 |
| DART 공시·뉴스 + 공시뉴스 탭 | Won't Have였음 |
| 코스피 전 종목 확대 | `1.6`이 **50종목 → 915종목** |
| KIS 전역 레이트리밋 큐(`reserveKisSlot`) | 없음 (`1.4`가 "토큰 캐시 + 1회 재시도" 2h로 잡혀 있었음) |

**그래서 위 10절의 28.0h 정산은 최종 결과와 무관해졌습니다.** 실제 소요 시간은 기록해두지 않아 정확한 숫자를 적을 수 없습니다 — **없는 숫자를 추정해서 적지 않겠습니다.**

### 11.3 개별 항목 정정

| WBS | 계획 | 실제 |
|---|---|---|
| `1.4` | `lib/kis.ts` = 토큰 발급 + 캐시 + 1회 재시도 (2h) | + 인메모리 캐시 + **초당 1건 전역 큐** + **엔드포인트 11개**. 2h를 크게 초과 |
| `1.6` | 코스피 **50종목** CSV 수동 Import | **915종목**, KIS 종목마스터(`kospi_code.mst.zip`) 파싱 + DART 고유번호 매핑 805건 |
| `2.6` | **`lib/wallet.ts`** | ❌ 그런 파일 없음. `lib/portfolio.ts`(`getMyPortfolio`)가 대체하고, 지갑 생성은 `/api/order`에 인라인 |
| `3.x` | 라우트 7개 | **15개** — `/api/budget`·`/api/popular`·`/api/chart`·`/api/quotes`·`/api/dividend`·`/api/financial-ratio`·`/api/disclosures`·`/api/order/[id]/cancel`·`/api/order/[id]/check-fill` 추가 |
| `3.7` | 14단계, 상태코드 400/401/422/404/409/502 | 실제 15단계. **거부는 대부분 HTTP 200 + `{ok:false, code}`** (화면이 네트워크 오류와 업무 거부를 구분할 필요가 없게) |
| `4.2` | 당시가 vs 현재가 (선행 `3.3`) | 선행이 `3.3`이 아니라 신설 `/api/quotes`(20종목 상한, `timedOut` 플래그) |
| `5.4` | `event_logs`에 **12종** | 정의 **15종**, DB 적재 12종. 미구현 2종·계획에 없던 3종 ([[../03_Data_Event/03_event_catalog|이벤트 카탈로그]] 4절) |
| `7.1` | 루트 README 작성 | ✅ 8/7 작성 (그전까지 create-next-app 보일러플레이트였음) |
| — | AI-3 용어 설명 (Should) | ❌ 끝까지 못 만들었습니다 |

---

## 관련 문서

- [[02_milestones|4일 마일스톤과 위험]]
- [[../05_Scope/01_mvp_scope|MVP 범위]]
- [[../04_Architecture/02_architecture|아키텍처]]
