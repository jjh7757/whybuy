# 완료 기준과 체크리스트

- 작성일: 2026-08-06

---

## 0. 이 문서를 쓰는 이유

**"거의 다 됐습니다"를 금지하기 위해서입니다.**

완료 기준이 없으면 발표 전날에 "동작은 하는데 이것만 고치면"이 반복됩니다. 각 항목은 **예/아니오로만 답할 수 있게** 썼습니다.

### 🔄 8/7 판정 표기

| 표기 | 뜻 |
|---|---|
| ✅ | 코드로 확인된 달성 |
| ⚠️ | **부분 달성** — 조건이 붙습니다 |
| ❌ | 미달성, 또는 **기준 자체가 낡아 판정 불가** |
| ❓ | **검증하지 않음** — 산출물이 없어 예/아니오를 말할 수 없습니다 |

> 🔴 **❓를 ✅로 적지 않습니다.** 이 문서의 목적이 "거의 다 됐습니다"를 금지하는 것인데, 확인 안 한 것을 달성으로 적으면 그 목적이 무너집니다. **❓ 항목은 Day 4에 실제로 해봐야 합니다.**

---

## 1. 🔴 요구사항 완료 기준 (제출 필수)

### REQ-01 :: 주제

- [ ] 프로젝트 2 기획서를 기반으로 했음을 문서에 명시했습니다.
- [ ] **웹이기 때문에 가능해진 것 5가지**를 문서에 적었습니다. ([[../01_Baseline/01_project_statement|한 문장]] 4절)
- [ ] **무엇을 뺐는지와 그 이유**를 제출 문서에 적었습니다. ([[../07_Submit/02_scope_reduction|범위 축소 설명서]])

### REQ-02 :: AI 기능

- [x] ✅ AI-1: 계좌 숫자를 넣으면 **매번 다른 해석 문장**이 나옵니다.
- [x] ✅ AI-1: 종목 시세를 넣으면 해석 문장이 나옵니다. 🔄 공시·뉴스가 있으면 `최근 소식` 구획이 추가됩니다.
- [x] ✅ AI-2: 근거 목록을 넣으면 회고 코멘트가 나옵니다.
- [x] ✅ 🔴 **미리 만들어 둔 응답이 아닙니다.** 매 호출마다 실데이터로 프롬프트를 구성합니다.
- [ ] ❓ AI 응답 30건을 검사해 **매수·매도 권유 표현이 0건**입니다.
- [ ] ❓ AI 응답 30건에 **목표가·수익률 예측이 0건**입니다.

> 검사 방법: `event_logs`에서 AI 호출 이벤트를 세고, 화면에서 실제 응답 30건을 눈으로 읽습니다.
>
> ❓ **아직 안 했습니다.** 프롬프트에 금지 규칙(`SAFETY_RULES`)을 걸어두었고 개발 중 눈에 띈 위반은 없었지만, **30건을 세어 읽은 산출물이 없습니다.** "규칙을 걸었다"와 "0건임을 확인했다"는 다른 말이므로 ✅로 적지 않습니다. Day 4에 실제로 세어야 합니다.

### REQ-03 :: 데이터 저장

- [x] ✅ Supabase에 테이블이 6개 있습니다. ⚠️ 같은 Supabase 프로젝트를 프로젝트 2와 재사용해 그쪽 테이블 3개가 함께 보입니다([[../03_Data_Event/01_data_structure|데이터 구조]] 0절).
- [x] ✅ 🔴 **주문을 넣고 브라우저를 완전히 종료한 뒤 다시 열어도 회고 화면에 그 주문이 있습니다.**
- [x] ✅ 근거(`rationales`)가 주문(`orders`)과 연결되어 저장됩니다.
- [x] ✅ 주문이 거부되어도 근거는 남아 있습니다. (근거 INSERT가 KIS 호출보다 **앞**)

> ✅ **실측**: 주문 8건, 근거 8건 — 기록률 8/8.

### REQ-04 :: 배포

- [x] ✅ 접속 가능한 Vercel URL이 있습니다. → https://whybuy-gray.vercel.app
- [ ] ❓ 🔴 **시크릿 창에서 열어 화면 3개가 모두 보입니다.**
- [ ] ❓ 🔴 **다른 기기(휴대폰)에서 열어 화면 3개가 모두 보입니다.**
- [ ] ❓ 🔴 **시크릿 창·휴대폰에서 Google 로그인이 실제로 완료되고 돌아옵니다.**
- [x] ✅ 로컬에서만 있는 환경변수가 없습니다. 🔄 `DART_API_KEY`가 8/7에 추가돼 Vercel에도 등록했습니다.
- [ ] ❓ 첫 로딩이 5초 안에 끝납니다.

> ✅ **리디렉션은 구조적으로 안전합니다.** `${location.origin}/auth/callback`으로 만들어 하드코딩된 도메인이 없습니다. 다만 **실제로 시크릿 창에서 로그인해본 적은 없습니다** — 요청서가 명시적으로 경고한 항목이므로 Day 4에 반드시 합니다.

### REQ-05 :: 최소한의 사용성

- [x] ✅ 첫 화면에 **이 서비스가 무엇을 하는지** 한 문장이 있습니다.
- [ ] ⚠️ ~~첫 화면에 **무엇을 먼저 눌러야 하는지** 버튼 3개가 있습니다.~~ 🔄 **첫 화면 카드는 2개**(종목 찾아보기·지난 근거)이고, 계좌는 카드가 아니라 화면에 인라인으로 펼쳐집니다. 이동 경로 3개는 **상단 내비게이션**이 담당하며 모든 화면에 항상 붙어 있습니다. 기준이 말한 "무엇을 눌러야 하는지 안다"는 충족되지만 **"버튼 3개"라는 형태는 다릅니다.**
- [x] ✅ 🔴 **Gemini API 키를 일부러 틀리게 해도 화면이 깨지지 않습니다.** `lib/gemini.ts`가 절대 throw하지 않고, 라우트가 200 + `ok:false`를 돌려주며, AI 영역만 대체 문구로 바뀝니다.
- [x] ✅ 🔴 **수량을 비운 채 주문하면** 아무 일도 일어나지 않고 안내 문구만 나옵니다.
- [x] ✅ 🔴 **근거를 고르지 않으면 주문 버튼이 눌리지 않습니다.** 서버도 화이트리스트로 독립 검증합니다.
- [x] ✅ 검색 결과가 0건일 때 지원 종목 예시가 함께 나옵니다.
- [ ] ⚠️ 데이터가 하나도 없는 상태(회고 화면 첫 방문)에서 안내 문구가 나옵니다. → **로그인한 경우에만.** 미로그인이면 로그인 안내가 대신 뜹니다(4절 참고).
- [x] ✅ 로딩 중임을 알 수 있습니다. 스켈레톤·스피너·로딩 문구가 각 영역에 있습니다.
- [x] 🆕 첫 방문자에게 **6단계 스포트라이트 튜토리얼**이 자동 실행됩니다. (계획에 없던 항목)

---

## 2. 🔴 안전 기준 (주문 관련)

**이 항목들이 지켜지지 않으면 기능이 아무리 많아도 완료가 아닙니다.**

- [x] ✅ 🔴 **근거를 고르지 않은 주문이 KIS에 도달하지 않습니다.**
  - 실측: 주문 8건 전부에 근거가 붙어 있습니다 (8/8)
- [ ] ⚠️ 🔴 **가상 예산을 초과하면 KIS가 호출되지 않습니다.**
  - ✅ 주문·매수가능조회는 확실히 차단되고 `orders` 행도 늘지 않으며 `order_rejected_budget_exceeded`가 기록됩니다.
  - ⚠️ **다만 시장가 주문은 예산 검증 *전에* 시세 조회로 KIS를 이미 1회 부릅니다.** 예상 금액을 계산하려면 현재가가 필요하기 때문입니다. "KIS가 호출되지 않는다"는 문구는 **주문 API 기준으로만** 맞습니다.
- [x] ✅ 🔴 **장 마감 시간에 주문하면 접수되지 않습니다.** 장 시간 검사가 KIS 호출·DB INSERT보다 **가장 앞**에 있습니다.
- [ ] ⚠️ 실제 계좌 예수금을 초과하는 주문이 차단됩니다. → **매수만.** 🔄 매도는 예수금이 아니라 **보유 수량**이 한도라 이 검증 자체를 타지 않습니다(설계상 의도).
- [x] ✅ 같은 주문을 연타해도 1건만 들어갑니다. 🔄 조건에 `side`가 추가됐습니다.
- [ ] ⚠️ 클라이언트에서 `qty`를 조작해도 서버가 시세를 재조회해 금액을 다시 계산합니다. → **시장가만.** 🔴 **지정가는 클라이언트가 보낸 `limitPrice`를 그대로 쓰고 호가단위 배수인지만 검증합니다.** 이건 알려진 한계입니다.
- [x] ✅ 계정 A로 주문해도 계정 B의 가상 예산은 줄지 않습니다. `getMyPortfolio(supabase, user.id)` + `user_wallets` + RLS 3중.
- [x] ✅ 로그인하지 않고 [주문하기]를 누르면 KIS가 호출되지 않습니다. 🔄 **Google 로그인 "버튼이 뜨는" 게 아니라 즉시 로그인으로 넘어갑니다.** 폼은 저장했다가 돌아오면 복원합니다.
- [ ] ❓ 🆕 **매도 주문을 실제로 눌러 KIS까지 나가는 것을 확인했습니다.** → 공유 모의계좌라 대신 눌러볼 계정이 없어 못 했습니다. 기존 실주문 데이터로 집계 로직만 대조했습니다.

---

## 3. 🔴 자격증명·개인정보 기준

- [x] ✅ `NEXT_PUBLIC_`이 붙은 환경변수가 **Supabase URL과 anon key 2개뿐**입니다. (전체 10개 중)
- [x] ✅ 브라우저에 `KIS_APP_KEY`가 노출되지 않습니다. `lib/kis.ts`에 `import "server-only"`가 있어 클라이언트가 import하면 **빌드가 실패**합니다.
- [x] ✅ 브라우저 Network 탭에 KIS·Gemini 도메인 호출이 **0건**입니다. 모든 외부 호출이 Route Handler 안에 있습니다.
- [x] ✅ 화면 어디에도 계좌번호가 표시되지 않습니다. `KIS_ACCOUNT_NO`는 `lib/kis.ts`에서만 쓰이고 응답 페이로드에 없습니다.
- [x] ✅ `event_logs`에 토큰·계좌번호가 없습니다. payload 전수 확인했고, 메모는 원문 대신 `has_memo` 불리언만 남습니다.
- [x] ✅ `.env.local`이 `.gitignore`에 있고 리포지토리에 올라가지 않았습니다. (`git ls-files` 0건)
- [ ] ❓ 🔴 **시연 영상과 발표자료 스크린샷에 계좌번호가 보이지 않습니다.**
- [x] 🆕 ✅ `SUPABASE_SERVICE_ROLE_KEY`를 쓰는 `lib/supabase/admin.ts`에도 `import "server-only"` 가드를 달았습니다. **8/7 전에는 주석만 있고 강제가 없었습니다.**

> ⚠️ **이 절에 새로 생긴 한계가 하나 있습니다.** `SUPABASE_SERVICE_ROLE_KEY`를 쓰기로 하면서 "service role key를 사용하지 않는다"던 계획이 깨졌습니다. 키 자체는 브라우저에 노출되지 않지만 **서버 코드 버그를 RLS가 막아주지 못합니다.** 발표에서 한계로 말합니다 — [[../03_Data_Event/01_data_structure|데이터 구조]] 7절.

> 🔴 마지막 항목은 녹화 후에 반드시 재생해서 확인합니다. 화면 구석에 잠깐 보이는 경우가 있습니다.

---

## 4. 화면별 완료 기준

### `/` 계좌 대시보드

- [ ] ❌ ~~**예수금**·총평가금액·평가손익이 표시됩니다.~~ 🔄 **기준 자체가 낡았습니다.** 8/6에 계좌 화면을 공용 KIS 잔고에서 **내 주문 기준**으로 갈아엎으면서 **예수금 개념이 사라졌습니다.** 지금 표시되는 것은 아래로 대체합니다.
- [x] ✅ **내 모의 투자금 / 주문에 쓴 금액 / 남은 예산**이 표시됩니다.
- [x] ✅ **내 보유 평가액 / 평가손익**이 표시됩니다.
- [x] ✅ 🆕 **대기중인 지정가 주문 목록**과 [체결 확인]·[취소] 버튼이 표시됩니다. (계획에 없던 항목)
- [x] ✅ 보유 종목이 있으면 목록이 표시됩니다. 🆕 클릭하면 그 종목 화면으로 이동합니다.
- [x] ✅ 보유 종목이 0개여도 화면이 깨지지 않습니다.
- [x] ✅ [이게 무슨 뜻인가요?] 버튼을 누르면 AI 해석이 나옵니다.
- [x] ✅ 화면 진입만으로는 AI가 호출되지 않습니다.
- [ ] ⚠️ 미로그인 상태에서는 **로그인 안내만** 뜹니다. 위 항목들은 전부 로그인 후 기준입니다.

### `/trade` 종목 조회와 주문

- [x] ✅ `삼성`으로 검색하면 후보가 나옵니다.
- [x] ✅ `삼성전자` 검색 시 `삼성전자우`가 함께 나오고 **자동으로 하나를 고르지 않습니다.**
- [x] ✅ 없는 종목을 검색하면 예시와 함께 안내가 나옵니다.
- [x] ✅ 종목을 고르면 현재가·등락률이 표시됩니다.
- [x] ✅ 수량을 입력하면 예상금액이 계산됩니다. 🔄 매도면 "예상 **정산**금액"으로 라벨이 바뀝니다.
- [ ] ⚠️ ~~근거 **6개**가 라디오 버튼으로 표시됩니다.~~ 🔄 **매수 6개 / 매도 5개**가 `side`에 따라 교체됩니다.
- [x] ✅ 메모를 비워도 주문이 진행됩니다.
- [x] ✅ 확인 모달에 "이 주문 후 남는 예산 OOO원"이 표시됩니다. 🔄 매도면 **더해진** 금액이 나옵니다.
- [x] ✅ 주문 성공 시 주문번호와 회고 화면 링크가 나옵니다. 🔄 지정가는 "접수되어 대기중입니다" 문구로 갈립니다.
- [ ] ⚠️ 가상 예산을 초과하는 수량을 입력하면 **주문 전에** 차단되고 남은 한도가 안내됩니다. → **클라이언트 사전 차단은 없습니다.** 수량 퀵버튼(10/25/50/100%)이 예산 안에서 계산해줄 뿐이고, 실제 차단은 서버가 합니다(200 + `budget_exceeded` + 남은 한도).
- [x] 🆕 시장가/지정가 토글이 있고, 지정가는 **호가단위**가 맞지 않으면 안내가 뜹니다. (계획에 없던 항목)
- [x] 🆕 보유한 종목에서만 **매수/매도 토글**이 나타납니다. (계획에 없던 항목)
- [x] 🆕 **탭 3개**(차트·호가 / 종목정보 / 공시뉴스)로 나뉘고, 주문 폼은 탭과 무관하게 계속 보입니다. (계획에 없던 항목)

### `/journal` 근거 회고

- [x] ✅ 주문 목록이 최신순으로 나옵니다.
- [x] ✅ 🔴 **다른 계정으로 로그인하면 이전 계정의 주문은 보이지 않습니다.** RLS `select own orders`.
- [ ] ❌ ~~로그인하지 않아도 (내가 아닌) 전체 서비스 소개와 빈 상태 화면은 볼 수 있습니다.~~ 🔄 **미로그인이면 로그인 안내만 뜹니다.** 회고 화면은 본질적으로 "내 기록"이라 로그인 전에 보여줄 내용이 없다고 판단해 이렇게 만들었는데, **이 기준과 어긋난다는 것을 인지하지 못한 채 지나갔습니다.** 기준 쪽을 접습니다 — 빈 상태 화면을 미로그인에게 보여줄 실익이 없습니다.
- [x] ✅ 각 행에 종목·수량·주문일·당시가·근거 유형·메모가 있습니다. 🆕 매수/매도 배지와 `대기중`·`취소됨` 상태 배지가 추가됐습니다.
- [x] ✅ 보유 종목의 현재가가 당시가와 함께 표시됩니다.
- [ ] ⚠️ 상단에 총 주문 수와 근거 기록률이 표시됩니다. → 🔄 **비율이 아니라 실수로 표시합니다**("근거 기록 8/8건"). 표본이 작아 비율이 오해를 낳기 때문이고, 이건 [[01_mvp_scope|MVP 범위]] 8절의 "정량은 참고치로만" 방침과 일치합니다.
- [x] ✅ [내 근거 되돌아보기]를 누르면 AI 코멘트가 나옵니다.
- [x] ✅ 🔴 **KIS를 끊어도 목록과 집계는 보입니다.** 서버 렌더 경로에 KIS 호출이 0회이고, 현재가만 `—`가 됩니다.
- [x] ✅ 데이터가 0건일 때 "아직 기록이 없습니다 + [첫 주문 해보기]"가 나옵니다. (로그인한 경우)

---

## 5. 발표 준비 완료 기준

- [ ] ❓ 🔴 **주문 시연 영상이 있습니다.** (Day 2 녹화) — 8/7에 녹화를 시도한 기록이 있으나 **파일 내용을 확인하지 못했습니다.** 재생해서 실제로 주문 장면이 담겼는지 봐야 합니다.
- [ ] ❓ 조회·회고 시연 영상이 있습니다. (Day 4 녹화)
- [ ] 발표 시각을 확인했습니다. → 09:00~15:30이면 주문 라이브 가능, 밖이면 영상 사용
- [ ] 시연 순서를 정했습니다: `첫 화면 → 계좌 → 종목 → 주문 → 회고`
- [ ] 리허설을 1회 했습니다.
- [ ] 슬라이드에 **무엇을 뺐는지**가 있습니다. (REQ-01)
- [ ] 발표에서 말할 숫자를 측정 SQL로 뽑아 적어 두었습니다.

---

## 6. 🔴 "완료"라고 말할 수 없는 상태

| 상태 | 왜 완료가 아닌가 |
|---|---|
| 로컬에서는 되는데 Vercel에서 안 됨 | REQ-04 미달. **배포된 것만 존재합니다** |
| 내 브라우저에서는 되는데 시크릿 창에서 안 됨 | 요청서가 명시적으로 경고한 상황입니다 |
| 근거 없이도 주문이 들어감 | 이 프로젝트의 존재 이유가 사라집니다 |
| AI가 가끔 권유 표현을 함 | 유사투자자문 위험 |
| 회고 화면이 비어 있음 | 웹으로 옮긴 이유가 없어집니다 |
| 데이터가 새로고침 시 사라짐 | REQ-03 미달 |

---

## 7. 일자별 게이트 (통과 못 하면 넘어가지 않음)

| Day | 게이트 | 🔄 결과 |
|---|---|---|
| Day 1 목 | ~~🔴 **Vercel 배포 URL에서 내 모의계좌 잔고가 보인다**~~ | ❌ **기준이 당일에 폐기됨** — 같은 날 저녁 공용 잔고 조회를 버리고 "내 주문 기준 예산"으로 바꿨습니다. 실질 게이트(배포 URL에서 KIS 데이터가 보인다)는 ✅ 통과 |
| Day 2 금 | 🔴 **주문 1건이 실제로 접수되고 근거가 저장되었다 + 시연 영상이 있다** | ✅ **하루 앞당겨 통과** — 8/6 15:16, 매수 주문번호 `0000041001`. 영상 여부는 ❓ |
| Day 3 토 | 회고 화면이 동작하고 예외 12종이 화면을 깨뜨리지 않는다 | ✅ 회고 화면은 8/6 완료. 🔄 예외는 12종이 아니라 **17종**이 됐습니다 |
| Day 4 일 | 🔴 **시크릿 창·휴대폰에서 화면 3개가 보인다 + 리허설 1회 완료** | ❓ **미완** — 남은 유일한 필수 게이트입니다 |

> **Day 1과 Day 2의 게이트가 절대적입니다.** 두 날을 놓치면 주말에 복구할 방법이 없습니다. ([[../06_WBS/02_milestones|마일스톤]])

### 🔴 8/7 시점에서 남은 것

**개발은 게이트 기준으로 전부 통과했고, 남은 것은 검증과 발표 준비입니다.**

| 남은 일 | 왜 필요한가 |
|---|---|
| ❓ 시크릿 창·휴대폰 검수 + 로그인 왕복 | 요청서가 명시적으로 경고한 항목. **Day 4 게이트** |
| ❓ AI 응답 30건 권유 표현 검사 | "규칙을 걸었다"와 "0건임을 확인했다"는 다른 말 |
| ❓ 측정 SQL 실행 및 발표 숫자 확정 | 표본이 계속 늘어 발표 직전에 뽑아야 함 |
| ❓ 리허설 1회 | |
| ❓ 매도 실주문 (장중) | 공유 계좌 문제로 못 했음. 안 되면 **한계로 명시** |

> ⚠️ **이 목록이 ❓로만 채워져 있다는 것 자체가 신호입니다.** 개발이 계획보다 이틀 빨리 끝났는데 남은 시간을 검증이 아니라 **새 기능**에 썼습니다([[../07_Submit/02_scope_reduction|범위 축소 설명서]] 3.5절). Day 4는 이 목록만 처리합니다.

---

## 관련 문서

- [[01_mvp_scope|MVP 범위와 우선순위]]
- [[../06_WBS/02_milestones|4일 마일스톤과 위험]]
- [[../07_Submit/01_readme_draft|README 초안]]
