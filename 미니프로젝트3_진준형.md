# 왜샀어 (WhyBuy)

## 0. 기본 정보
- 이름 : 왜샀어 (WhyBuy)
- 배포 주소 : https://whybuy-gray.vercel.app
- 사용한 AI 모델/API : Google Gemini API (`gemini-3.5-flash`, `generateContent` 엔드포인트 직접 호출) — 시세·계좌 해석 및 회고 코멘트 생성에 사용. 그 외 외부 API 3종 : 한국투자증권(KIS) Open API(모의투자)로 실제 시세 조회·매수/매도 주문 처리, **DART 공시 API**와 **Google 뉴스 RSS**로 종목별 최근 공시·뉴스 조회.

## 1. 한 줄 소개
투자 초보자가 모의투자로 실제 매매를 하되, 주문 전에 반드시 "왜 사는지/파는지" 근거를 남기게 해서 "그냥 감"으로 사고파는 습관을 줄여주는 서비스입니다.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 투자를 막 시작했거나 시작하려는 초보 투자자
- 그 사람이 지금 겪는 불편 : 뉴스·소문·감으로 일단 사고, 나중엔 왜 샀는지 기억을 못 해서 같은 실수를 반복함
- 이 서비스를 쓰면 달라지는 것 : 매수·매도 전에 정해진 근거(매수: 저평가/실적/업황/뉴스/배당/감, 매도: 손절/익절/목표가 도달/판단 변경/감) 중 하나를 반드시 고르게 되고, 나중에 회고 화면에서 "그때 얼마였고 왜 샀는지 → 지금 어떻게 됐는지"를 직접 대조해볼 수 있습니다.

## 3. 주요 기능 (3개 이내)
1. 근거를 남겨야 완료되는 매수·매도 주문 — KIS 모의투자 실계좌 연동, 시장가/지정가 모두 지원
2. 시세·계좌 화면에 대한 AI 해석 — 최근 공시·뉴스까지 재료로 넣되, 숫자가 무엇을 뜻하는지만 설명하고 매수·매도는 권유하지 않음. 같은 공시·뉴스를 사용자도 별도 탭에서 원문 그대로 볼 수 있어 AI 말을 검증할 수 있음
3. 회고 화면 — 과거 주문의 근거·당시 가격과 지금 가격을 비교하고, AI가 근거 편중("그냥 감" 비율 등)을 지적

## 4. AI를 어디에 어떻게 썼나요
- AI가 하는 일 : (1) 종목 시세 화면의 현재가·PER·PBR 등 숫자 해석 + 최근 공시·뉴스를 "있었던 일"로만 언급 (2) 계좌 화면의 예산·보유 종목 해석 (3) 최근 근거 20건을 보고 편중 지적 + 다음에 물어볼 질문 1개 제안(회고 화면)
- 입력으로 무엇을 주고, 무엇을 받나요 : 서버가 세 곳에서 모은 실제 숫자를 프롬프트에 그대로 채워 넣습니다 — 시세·지표는 KIS, 예산·보유 종목은 Supabase의 내 주문 기록 집계(KIS 계좌 잔고가 아닙니다. 모의계좌 1개를 여러 명이 나눠 쓰기 때문에 계좌 잔고에는 남의 주문이 섞여 있습니다), 공시·뉴스는 DART·Google. Gemini는 그 숫자를 설명하는 한국어 텍스트만 돌려받습니다. 매수·매도 여부 자체는 AI가 결정하지 않습니다.
- AI가 이상한 답을 줬을 때 어떻게 처리했나요 : Gemini 호출 함수는 절대 throw하지 않고 실패 시 `{ok:false}`만 반환합니다. 화면은 다른 부분은 그대로 둔 채 AI 영역만 "해석을 준비하지 못했습니다 + 다시 시도" 버튼으로 대체합니다. 또 모델이 요청한 출력 형식(`제목|내용`)을 안 지켜도 글이 사라지지 않도록, 형식 파싱 실패 → 빈 줄 기준 문단 분리 → 통짜 한 덩어리 순으로 떨어지는 폴백 체인을 뒀습니다.
- 프롬프트에서 신경 쓴 점 1가지 : 모든 프롬프트 앞에 공통 금지 규칙을 붙여 "특정 종목 매수·매도 권유 금지, 목표가·수익률 예측 금지, 사용자 판단이 맞다/틀렸다 평가 금지, 숫자의 의미를 설명하는 데까지만" 답하게 강제했습니다. PER·PBR 해석에는 "높다/낮다"까지는 말해도 "싸다/비싸다·고평가/저평가"로 단정하지 못하게 한 번 더 별도로 못박았습니다(같은 값도 업종·시점에 따라 뜻이 달라진다는 점을 반드시 덧붙이게 함).

## 5. 데이터 설계
- 테이블 이름과 역할 : `stocks`(종목 마스터, 코스피 전 종목) · `orders`(주문) · `rationales`(판단 근거) · `user_wallets`(사용자별 가상 예산) · `kis_tokens`(KIS 토큰 캐시) · `event_logs`(이벤트 기록)
- 주요 컬럼 : `orders.expected_price`(주문 당시 가격, 회고의 핵심) · `orders.side`/`order_type`/`limit_price`/`filled_qty`/`filled_price`(매수·매도 + 시장가·지정가 + 부분체결 추적) · `rationales.reason_type`/`reason_memo`(근거와 메모) · `user_wallets.allocated_amount`(가상 예산) · `stocks.dart_corp_code`(DART 고유번호 — 공시 조회가 종목이 아니라 회사 단위 API라 별도 매핑이 필요했고, 915종목 중 805개가 매핑됨. 누락 110개는 전부 우선주로 원래 회사 단위 공시가 따로 없음)
- 이렇게 설계한 이유 : ① `orders`에 종목명을 중복 저장 — 종목명이 바뀌거나 상장폐지돼 `stocks`에서 사라져도 "그때 무엇을 샀는지" 기록은 그대로 남아야 하므로 정규화보다 기록의 불변성을 우선함. ② 남은 예산을 컬럼으로 캐시하지 않고 매번 `orders` 합계로 계산 — 캐시 값과 실제 주문 합계가 어긋나는 사고를 원천 차단. ③ `kis_tokens`는 RLS를 켜고 정책은 아예 두지 않아 anon key로는 읽을 수 없고 서버 전용 클라이언트만 접근 가능.

## 6. 화면 흐름
1. 첫 화면에서 예산 카드를 봅니다(로그인 전이면 안내만, 로그인하면 500만원 가상 투자금·남은 예산·보유 종목 평가손익 확인).
2. "종목 찾아보기"에서 검색하거나 인기 종목(코스피 거래대금 순) 중 하나를 고릅니다. **처음 온 사람에게는 실제 화면 요소를 짚어주는 6단계 스포트라이트 튜토리얼이 자동으로 실행**됩니다("둘러보기 다시 보기"로 재실행 가능).
3. 종목 화면은 탭 3개입니다 — **차트·호가**(종가 그래프·오늘/52주 범위·PER·PBR 등 지표), **종목정보**(재무비율·배당), **공시뉴스**(DART 공시·뉴스 원문 목록). 차트·호가 탭에서 "이게 무슨 뜻인가요?"를 눌러 AI 해석을 봅니다.
4. 수량(퀵버튼 지원)과 시장가/지정가를 정하고, 근거를 반드시 고른 뒤(메모는 선택) 주문 버튼을 누릅니다.
5. 로그인 전이라면 이 시점에 폼을 저장해두고 Google 로그인으로 넘어갔다가, 돌아오면 저장된 폼이 복원되며 확인 모달이 이어집니다. 확인 모달에서 예상 금액과 "이 주문 후 남는 예산"을 보고 최종 확인합니다.
6. 시장가는 즉시 체결 처리되고, 지정가는 "내 계좌"에서 체결 확인·취소를 합니다. 보유 종목을 클릭하면 그 종목 화면으로 바로 이동해 매도도 할 수 있습니다.
7. "지난 근거" 화면에서 그때 가격·근거와 지금 가격을 대조하고, AI의 근거 편중 지적을 봅니다.

## 7. 기획서와 달라진 점
- 뺀 것 : 자연어 채팅 인터페이스, 실시간 알림/자동 체결 감시(스케줄러), 캔들·이동평균선 같은 기술적 분석 도구, 종목 추천·목표가 제시, 실전 투자 계좌 연동. (**매도 주문·체결 조회·뉴스공시 수집 3가지는 원래 뺐다가 개발 중 다시 넣었습니다** — 아래 참고)
- 뺀 이유 : 자연어 채팅은 웹 폼보다 느리고 부정확해서 이 서비스엔 안 맞았습니다. 자동 감시는 Vercel에 상주 프로세스가 없어 구조적으로 불가능합니다. 캔들·지표는 초보자가 못 읽는 기호이고, 읽는 법을 가르치는 것 자체가 "판단 근거를 직접 설명하게 한다"는 목적 밖입니다. 종목 추천은 유사투자자문 영역이라 목적과 반대 방향입니다.
- 뺐다가 다시 넣은 3가지, 그리고 원래 뺀 이유 중 살아남은 것 :
  - **매도 주문** — 뺀 이유는 "근거 체계가 매수와 다르다"(손절·익절은 저평가·실적과 대응 안 됨)였는데, 근거 목록만 따로 만들면 해소되는 문제였지 영구히 못 만들 이유는 아니었습니다. 매도 근거 5종을 신설해 넣었습니다.
  - **체결 조회** — 뺀 이유는 "Vercel에 상주 프로세스가 없다"였고 **이건 지금도 유효합니다.** 그래서 스케줄러로 자동 감시하는 대신, 사용자가 "내 계좌"에서 버튼을 눌렀을 때만 KIS에 한 번 물어보는 방식으로 넣었습니다. 시장가 주문은 여전히 접수 즉시 완료로 취급하고, 지정가일 때만 이 경로를 탑니다.
  - **뉴스·공시 수집** — 뺀 이유는 "외부 데이터 범위가 크다"였는데, 종목당 공시 3건·뉴스 3건으로 좁히니 감당할 크기였습니다. 처음엔 AI 해석의 재료로만 쓰려다가, **AI만 원문을 보면 사용자가 AI 말을 검증할 수 없다**는 문제를 발견해 공시뉴스 탭으로 사용자에게도 열었습니다. "AI가 대신 판단해주지 않는다"는 이 서비스의 원칙과 정면으로 어긋나는 설계였기 때문입니다.
- 시간이 더 있었다면 넣었을 것 : 코스피 전 종목(915개)을 다 넣긴 했지만 KIS 모의투자에서 실제로 시세·주문이 되는지는 전수 검증하지 못했는데, 그 검증. 매도까지 포함한 완전한 수익 실현 지표. 근거 유형별 수익률 집계.

## 8. 만들면서 가장 오래 막혔던 것
- 문제 : KIS가 앱키 하나당 초당 1건까지만 API 호출을 허용하는데(`EGW00201` 초당 거래건수 초과), 처음엔 이걸 모르고 기능마다 따로 부딪혔습니다 — 매수 직후 매수가능조회가 이어지는 주문 API, 인기 종목 목록, 분봉 차트, 배당 조회 등에서 각각 새로고침 두 번 만에 걸릴 정도였습니다.
- 어떻게 해결했나 : 기능별로 재시도·캐시를 따로 넣던 방식을 그만두고, 모든 KIS 호출이 공통으로 거치는 지점(`callKis`)에 "마지막 호출로부터 최소 1.1초는 기다렸다가 나간다"는 전역 큐(`reserveKisSlot`) 하나로 근본 해결했습니다. 다만 분봉 차트처럼 한 요청 안에서 최대 14번을 연속 호출해야 하는 경우엔 이 큐 때문에 응답이 오래 걸리는 새 문제가 생겼는데, 이건 불필요하게 반복되던 토큰 재조회를 없애고 Vercel 함수 리전을 한국(icn1)으로 옮겨 KIS와의 왕복 지연을 줄이는 걸로 완화했습니다.

## 9. 실제 서비스로 발전시킨다면

### 지금 이 형태의 진짜 강점
**계좌 개설 없이, 구글 로그인 하나로 즉시 진짜 증권사 API 기반 모의투자를 시작할 수 있다는 것**입니다. 시중의 "모의주식" 앱들은 대부분 자체 서버가 가짜 잔고·가짜 체결을 흉내 내는 순수 시뮬레이터인데, 이 서비스는 다릅니다 — 주문을 넣으면 실제로 한국투자증권(KIS) API를 통해 진짜 증권사 계좌·진짜 주문 처리 파이프라인을 타고, 진짜 주문번호가 발급됩니다. 그런데도 사용자는 KIS에 별도로 가입하거나 계좌를 틀 필요 없이 구글 로그인 한 번으로 500만원 모의 투자금을 받아 바로 시작합니다. "진짜 증권사 인프라의 신뢰성"과 "가입 절차 없는 즉시성"을 동시에 잡은 조합이 이 프로젝트의 가장 뾰족한 지점이라고 판단했습니다.

### 하지만 이 조합이 가능한 이유 자체가 확장의 벽입니다
이게 가능한 이유는 **모의계좌 1개를 여러 사용자가 가상 예산으로 나눠 쓰기 때문**입니다. 자체 조사 결과, 이 구조는 자본시장법과 부딪힐 소지가 있습니다.

- **제6조③**: 투자중개업은 "**타인의 계산으로** 금융투자상품의 매도·매수... 을 **영업으로 하는 것**"으로 정의됩니다.
- **제11조**: 인가 없이 금융투자업(투자중개업 포함)을 영위할 수 없습니다.
- **제11조의2** (2021년 신설): 무인가 영업을 목적으로 **계좌의 대여를 알선·중개**하는 것 자체도 금지됩니다.
- **제444조**: 두 조항 위반 시 **5년 이하 징역 또는 2억원 이하 벌금**입니다.

"가상의 돈이니까 괜찮지 않냐"는 가설도 검증해봤습니다. 한국거래소(KRX)가 직접 "모의증권투자게임"을 공개 운영하고, 언론사 주최 모의투자대회도 흔한 걸 보면 **가상의 돈으로 하는 모의투자 자체는 분명히 안전한 영역**입니다. 다만 그런 사례들은 전부 **자체 서버가 가짜 잔고를 관리하는 순수 시뮬레이터**이고, 이 서비스는 실제로 KIS의 진짜 계좌 인프라(모의투자 계좌도 실계좌와 동일한 형식의 고유 계좌번호가 발급되고, 진짜 주문번호가 나감)를 여러 명이 나눠 쓰는 구조라 성격이 다릅니다. 즉 위험의 기준은 "돈이 가짜냐"가 아니라 **"한 사람 명의의 실제 계좌 인프라를 불특정 다수가 실제로 공유하냐"**로 보입니다. (이 구분을 금융당국·법원이 명시적으로 그렇게 판단한 선례를 직접 확인하진 못했고, 확정된 법률 해석이 아니라 조문·선례를 근거로 한 추론입니다.)

### 확장 경로 3가지
| 방향 | 라이선스 문제 | 대신 잃는 것 |
|---|---|---|
| 사용자별 실제 KIS 계좌를 OAuth로 연동 | 해소됨 (본인 계좌를 본인이 씀) | "설정 없이 즉시 시작"이라는 지금의 편의성 |
| 실주문 없이 시세만 받아 자체 체결 엔진으로 전환 | 해소됨 (계좌 인프라 자체를 안 씀) | "진짜 계좌에 진짜 주문이 들어간다"는 차별점 |
| 증권사와 정식 제휴해 화이트라벨 프런트엔드로 편입 | 해소됨 (라이선스 있는 파트너 뒤에 섬) | 개인 프로젝트 단계에서 바로 갈 수 없음(제휴 필요) |

### 이번 과제에서의 판단
셋 중 하나를 미리 구현하기보다, 이 한계를 명확히 인지한 상태로 **학습·포트폴리오 목적의 기간제 데모**로 운영하는 쪽을 택했습니다. "시간이 없어서 못 했다"가 아니라, 지금 형태의 강점(계좌 개설 없는 즉시성)과 확장 가능성(사용자별 계좌 연동 등)이 서로 트레이드오프 관계에 있다는 것을 알고 설계했다는 뜻입니다.
